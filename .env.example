ETHERSCAN_API_KEY=your_etherscan_v2_api_key_here

# Optional. Default chainid; defaults to 1 (Ethereum mainnet) if unset.
DEFAULT_CHAIN=1

# Only needed to run the eval suite (`npm run eval`). Not used by the server.
ANTHROPIC_API_KEY=
